# Ably API key used by the server to mint short-lived client tokens
# (see src/lib/ably.functions.ts). Get one at https://ably.com — free tier
# is plenty for testing.
#
# Format: "appId.keyId:keySecret"
ABLY_API_KEY=
